model CooperativeRequestSchedule {
  @@map("cooperative_request_schedule")
  id                    String              @id @default(uuid()) @map("id")
  cooperativeRequestId  String  @unique             @map("cooperative_request_id")
  from                  DateTime            @map("from")
  expectationEnd        DateTime            @map("expectation_end")
  createdAt             DateTime            @map("created_at")
  updatedAt             DateTime            @map("updated_at")
  deletedAt             DateTime?           @map("deleted_at")

  cooperativeRequest    CooperativeRequest  @relation(fields: [cooperativeRequestId], references: [id])
}

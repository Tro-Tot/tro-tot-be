model Otp {
  id        Int       @id @default(autoincrement())
  otp       String
  email     String
  createdAt DateTime  @default(now())
  updatedAt DateTime
  deletedAt DateTime?
  expiredAt DateTime
}
